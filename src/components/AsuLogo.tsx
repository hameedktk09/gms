import React from 'react';

export const AsuLogo = ({ className }: { className?: string }) => (
  <img 
    src="https://raw.githubusercontent.com/hameedktk09/cms/main/ASU-logo.png" 
    alt="asu-logo" 
    className={className}
    referrerPolicy="no-referrer"
  />
);
